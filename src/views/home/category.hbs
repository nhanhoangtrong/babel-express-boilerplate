{{!< base }}

<div class="inner">
    <header>
        <h1>{{postCategory.name}}</h1>
        <p>{{postCategory.description}}</p>
    </header>

    <section class="box">
        <h2>There are {{posts.length}} posts in this category.</h2>
        {{> post-list }}
    </section>
</div>
